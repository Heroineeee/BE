spring:
  data:
    redis:
      host: ${LOCAL_REDIS_HOST}
      port: ${REDIS_PORT}

springdoc:
  swagger-ui:
    path: /swagger-ui/index.html
    url: ${LOCAL_SWAGGER_URL}
  api-docs:
    path: /v3/api-docs